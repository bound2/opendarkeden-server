# Factories that PacketFactoryManager::init() registers but that are
# deliberately NOT in the wire-layout inventory, one per line, each with a
# reason. tests/ratchet/ratchets.sh treats these as covered.
#
# (No entries at the moment: the CR*/RC* ranger/remote-admin relics that
# used to live here were deleted along with src/Core/Rpackets.)
