#!/bin/sh
# Wrapper: `zig ar` is two words. See cmake/zig-cc for how Zig is located.
set -e
if [ -n "${ZIG}" ]; then exec "${ZIG}" ar "$@"
elif command -v zig >/dev/null 2>&1; then exec zig ar "$@"
else exec python3 -m ziglang ar "$@"; fi
