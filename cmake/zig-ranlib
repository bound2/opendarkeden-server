#!/bin/sh
# Wrapper: `zig ranlib` is two words. See cmake/zig-cc for how Zig is located.
set -e
if [ -n "${ZIG}" ]; then exec "${ZIG}" ranlib "$@"
elif command -v zig >/dev/null 2>&1; then exec zig ranlib "$@"
else exec python3 -m ziglang ranlib "$@"; fi
