//--------------------------------------------------------------------------------
//
// Filename   : Assert1.h
// Written By : Reiot
//
// Historical near-duplicate of Assert.h (it declared the same entry points and
// defined the same macros). It is included by ~116 files, so it stays as a
// forwarder instead of being deleted; Assert.h is now the single definition.
//
//--------------------------------------------------------------------------------

#ifndef __ASSERT1_H__
#define __ASSERT1_H__

#include "Assert.h"

#endif
